node_modules/
dist/
coverage/
*.log
.DS_Store

# Superpowers plugin working docs (specs, plans) and brainstorm sessions.
# These are process artifacts; our durable design docs live in docs/*.md.
docs/superpowers/
.superpowers/

# Agent tooling artifacts (vendored skills cache) — keep local, not in git
.agents/
skills-lock.json
